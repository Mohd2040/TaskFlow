# kubernetes/helm/taskflow-app/values.yaml

backend:
  image:
    repository: ghcr.io/mohd2040/taskflow/taskflow-backend
    tag: sha-2b1c31beba592203e7c87f9b03ead87874ac2610 # تأكد أن هذا هو الـ SHA الصحيح أو أحدث Tag
    pullPolicy: IfNotPresent
  service:
    type: LoadBalancer
    port: 5000
  env:
    # ✅ تم حذف MONGODB_URL و JWT_SECRET من هنا
    # MONGO_URI: "..."
    # JWT_SECRET: "..."